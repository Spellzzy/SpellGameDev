---
description: 测试与调试阶段的执行规则。当用户报告Bug或需要代码审查时触发。
alwaysApply: false
enabled: true
updatedAt: 2026-03-02T14:13:17.318Z
provider: 
---

# 测试与调试阶段规则

当用户报告Bug、请求代码审查、或需要测试时执行。

## Bug 排查 SOP

### Step 1: 收集信息
必须确认以下4项（缺哪项问哪项）：
1. **现象**：看到了什么
2. **复现步骤**：1-2-3 步骤
3. **预期行为**：应该怎样
4. **实际行为**：实际怎样

### Step 2: 定位范围
- 从报错日志/堆栈 → 定位到文件和行号
- 从调用链向上追溯 → 找到根因
- 不要盲猜！先缩小范围再分析

### Step 3: 修复
- 说明根因
- 给出修复代码
- 说明修复可能影响的其他地方
- 建议如何验证修复是否有效

## 代码审查 SOP

审查时关注以下维度（按优先级）：

| 优先级 | 维度 | 关注点 |
|--------|------|--------|
| P0 | 正确性 | 逻辑错误、空引用、死循环、竞态 |
| P1 | 内存安全 | 事件没 Unsubscribe、协程没 Stop、引用没释放 |
| P2 | 性能 | Update 中 new、GetComponent、Find、频繁 GC |
| P3 | 可维护性 | 命名、结构、注释、单一职责 |
| P4 | 可扩展性 | 硬编码、耦合度、开闭原则 |

输出格式：
```
🔴 [严重] 文件名:行号 - 问题描述 → 修复建议
🟡 [建议] 文件名:行号 - 问题描述 → 改进建议
🟢 [良好] 值得保持的实践
```

## 性能审查要点

Unity 常见性能陷阱检查清单：
- [ ] Update 里有没有 new 对象
- [ ] 有没有每帧调用 GetComponent / Find / FindObjectOfType
- [ ] 字符串拼接是否用 + 号
- [ ] LINQ 在热路径中的使用
- [ ] 频繁的 SetActive 导致的 Canvas rebuild
- [ ] 物理碰撞的层级过滤是否合理
- [ ] 协程是否有泄漏（启动了没停止）
- [ ] 委托/事件是否有泄漏（订阅了没取消）